{
  "name": "watt-events-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd client && npm start",
    "build": "cd client && npm run build",
    "install": "cd client && npm install"
  }
}